/**
 * Character module entry point (docs/design/08 §3–4). Owned by the character work.
 * Returns null until implemented; the presenter then uses debug skeleton actors.
 */
import type { CharacterFactory } from '../contract';

export function createCharacterFactory(): CharacterFactory | null {
  return null;
}
