# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# legacy v3 corpus output (the v3 generator was retired; kept ignored so an old
# checkout's local corpus is never committed by accident)
public/replays/
exports/

# tooling
coverage/
.vite/
.eslintcache

# editors and OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# logs
*.log
npm-debug.log*
docs/DESIGN_FULL.md

# raw third-party downloads for scripts/assets/* (only converted outputs are committed)
.cache/

# calibration batch runs (scripts/batch); reports are regenerated from results.jsonl
runs/
